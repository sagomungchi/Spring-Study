<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- HandlerMapping 등록 -->
    <bean class="org.springframework.web.servlet.handler.SimpleUrlHandlerMapping">
        <property name="mappings">
            <props>
                <prop key="/login.do">login</prop>
                <prop key="/logout.do">logout</prop>
                <prop key="/getBoardList.do">getBoardList</prop>
                <prop key="/getBoard.do">getBoard</prop>
                <prop key="/insertBoard.do">insertBoard</prop>
                <prop key="/updateBoard.do">updateBoard</prop>
                <prop key="/deleteBoard.do">deleteBoard</prop>
            </props>
        </property>
    </bean>
    
    <!-- Resource Mapping -->
    <mvc:resources mapping="/SpringMVC/**" location="/SpringMVC/"/>

    <!-- Controller 등록-->
    <bean id="login"        class="board.view.user.LoginController"/>
    <bean id="logout"       class="board.view.user.LogoutController"/>
    <bean id="getBoardList" class="board.view.board.GetBoardListController"/>
    <bean id="getBoard"     class="board.view.board.GetBoardController"/>
    <bean id="insertBoard"  class="board.view.board.InsertBoardController"/>
    <bean id="updateBoard"  class="board.view.board.UpdateBoardController"/>
    <bean id="deleteBoard"  class="board.view.board.DeleteBoardController"/>

    <!-- ViewResolver 등록-->
    <bean id="viewResolver"
          class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/board/"/>
        <property name="suffix" value=".jsp"/>
    </bean>
</beans>